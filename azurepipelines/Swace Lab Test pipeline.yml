name: Swace Lab Test pipeline
trigger: none
parameters:
  - name: environment
    displayName: 'Select Test Environment'
    type: string
    default: 'UAT'
    values: 
      - 'DEV'
      - 'UAT'
      - 'QA'
      - 'STG'
      - 'PROD'
  - name: Scope
    displayName: 'Test Scope'
    type: string
    default: 'sanity'
    values:
      - 'sanity'
      - 'smoke'
      - 'regression'
      - 'Patching'
      - 'E2E'

variables:
  - group: playwright-secrets   # ADO Library > Variable Groups: WINDOWS_AUTH_USERNAME, WINDOWS_AUTH_PASSWORD

pool:
  vmImage: 'ubuntu-latest'

jobs:
  - job: ValidateParameters
    displayName: 'Validate Parameters'
    steps:
      - script: |
          echo "##[error]E2E scope is not supported for DEV or QA environments."
          exit 1
        displayName: 'Block E2E on DEV/QA'
        condition: |
          and(
            eq('${{ parameters.Scope }}', 'E2E'),
            in('${{ parameters.environment }}', 'DEV', 'QA')
          )

      - script: |
          echo "##[error]Regression scope is not supported for PROD environment."
          exit 1
        displayName: 'Block regression on PROD'
        condition: |
          and(
            eq('${{ parameters.Scope }}', 'regression'),
            eq('${{ parameters.environment }}', 'PROD')
          )

  - job: PlaywrightTests
    displayName: 'Execute Tests (${{ parameters.environment }} - ${{ parameters.Scope }})'
    dependsOn: ValidateParameters
    condition: |
      not(
        or(
          and(
            eq('${{ parameters.Scope }}', 'E2E'),
            in('${{ parameters.environment }}', 'DEV', 'QA')
          ),
          and(
            eq('${{ parameters.Scope }}', 'regression'),
            eq('${{ parameters.environment }}', 'PROD')
          )
        )
      )
    steps:
      - checkout: self

      - task: NodeTool@0
        inputs:
          versionSpec: '22'
        displayName: 'Setup Node.js'

      - script: npm ci
        displayName: 'Install dependencies'

      - script: npx playwright install --with-deps chromium
        displayName: 'Install Playwright browsers'

      - script: npx bddgen
        displayName: 'Generate BDD tests'

      # 👇 only runs when scope is regression to clean stale blob files
      - script: rm -rf blob-report
        displayName: 'Clean blob report folder'
        condition: eq('${{ parameters.Scope }}', 'regression')

      - script: |
          echo "##vso[task.setvariable variable=SmokeStatus]SKIPPED"
          echo "##vso[task.setvariable variable=SanityStatus]SKIPPED"
          echo "##vso[task.setvariable variable=RegressionStatus]SKIPPED"
        displayName: 'Initialize regression stage status'
        condition: eq('${{ parameters.Scope }}', 'regression')

      - script: |
          npm run test:smoke
          if [ $? -eq 0 ]; then
            echo "##vso[task.setvariable variable=SmokeStatus]PASSED"
          else
            echo "##vso[task.setvariable variable=SmokeStatus]FAILED"
          fi
          exit 0
        displayName: 'Run smoke gate'
        condition: eq('${{ parameters.Scope }}', 'regression')
        env:
          TEST_ENV: '${{ parameters.environment }}'
          WINDOWS_AUTH_USERNAME: $(WINDOWS_AUTH_USERNAME)
          WINDOWS_AUTH_PASSWORD: $(WINDOWS_AUTH_PASSWORD)

      - script: |
          npm run test:sanity-only
          if [ $? -eq 0 ]; then
            echo "##vso[task.setvariable variable=SanityStatus]PASSED"
          else
            echo "##vso[task.setvariable variable=SanityStatus]FAILED"
          fi
          exit 0
        displayName: 'Run sanity gate'
        condition: and(eq('${{ parameters.Scope }}', 'regression'), eq(variables['SmokeStatus'], 'PASSED'))
        env:
          TEST_ENV: '${{ parameters.environment }}'
          WINDOWS_AUTH_USERNAME: $(WINDOWS_AUTH_USERNAME)
          WINDOWS_AUTH_PASSWORD: $(WINDOWS_AUTH_PASSWORD)

      - script: |
          npx playwright test "Requests" "Account Services Manager" --workers=8 --fully-parallel
          if [ $? -eq 0 ]; then
            echo "##vso[task.setvariable variable=RegressionStatus]PASSED"
          else
            echo "##vso[task.setvariable variable=RegressionStatus]FAILED"
          fi
          exit 0
        displayName: 'Run regression suites'
        condition: and(eq('${{ parameters.Scope }}', 'regression'), eq(variables['SmokeStatus'], 'PASSED'), eq(variables['SanityStatus'], 'PASSED'))
        env:
          TEST_ENV: '${{ parameters.environment }}'
          WINDOWS_AUTH_USERNAME: $(WINDOWS_AUTH_USERNAME)
          WINDOWS_AUTH_PASSWORD: $(WINDOWS_AUTH_PASSWORD)

      - script: npm run test:${{ parameters.Scope }}
        displayName: 'Execute Tests (${{ parameters.environment }} - ${{ parameters.Scope }})'
        condition: ne('${{ parameters.Scope }}', 'regression')
        env:
          TEST_ENV: '${{ parameters.environment }}'
          WINDOWS_AUTH_USERNAME: $(WINDOWS_AUTH_USERNAME)
          WINDOWS_AUTH_PASSWORD: $(WINDOWS_AUTH_PASSWORD)

      - script: |
          echo "Smoke test $(SmokeStatus)."
          echo "Sanity test $(SanityStatus)."
          echo "Regression test $(RegressionStatus)."

          if [ "$(SmokeStatus)" != "PASSED" ]; then
            echo "##vso[task.logissue type=error]Smoke test failed in ${{ parameters.environment }}."
            exit 1
          fi

          if [ "$(SanityStatus)" != "PASSED" ]; then
            echo "##vso[task.logissue type=error]Sanity test failed in ${{ parameters.environment }}."
            exit 1
          fi

          if [ "$(RegressionStatus)" != "PASSED" ]; then
            echo "##vso[task.logissue type=error]Regression test failed in ${{ parameters.environment }}."
            exit 1
          fi
        displayName: 'Report failed test scope'
        condition: eq('${{ parameters.Scope }}', 'regression')

      - script: |
          echo "##vso[task.logissue type=error]${{ parameters.Scope }} tests failed in ${{ parameters.environment }}."
          echo "##vso[task.logissue type=warning]Check Publish test results and the playwright-report artifact for details."
        displayName: 'Report failed test scope (non-regression)'
        condition: and(ne('${{ parameters.Scope }}', 'regression'), failed())

      - task: PublishTestResults@2
        displayName: 'Publish test results'
        condition: always()
        inputs:
          testResultsFormat: 'JUnit'
          testResultsFiles: 'test-results/junit.xml'
          searchFolder: '$(System.DefaultWorkingDirectory)'
          mergeTestResults: true
          failTaskOnFailedTests: true

      - task: PublishPipelineArtifact@1
        displayName: 'Publish Playwright HTML report'
        condition: always()
        inputs:
          targetPath: '$(System.DefaultWorkingDirectory)/playwright-report'
          artifact: 'playwright-report'
          publishLocation: 'pipeline'